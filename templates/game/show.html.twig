{% extends 'base.html.twig' %}

{% block title %}Game{% endblock %}

{% block body %}
    <h1>Hex</h1>
    <table class="table">
        <tbody>
<tr> <th>Partie n°</th>
                <td>{{ game.id }}</td></tr>
<tr>
                <th>Partie commencée le :</th>
                <td>{{ game.createdAt ? game.createdAt|date('d-m-Y H:i') : '' }}</td></tr>
                  </tbody>
    </table>
          
      

    {{ include('game/_delete_form.html.twig') }}
{% endblock %}
